gradle.afterProject { project ->
    if (project.path == ':app') {
        project.android.defaultConfig.applicationId = 'com.codexswitch.mobile.historybench'
        project.extensions.getByName('react').entryFile =
            new File(project.rootDir, '../e2e/historyWorkerBenchmark.tsx')
    }
}
